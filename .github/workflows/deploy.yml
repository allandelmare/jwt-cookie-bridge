name: Deploy Plugin
on:
  push:
    tags:
      - "v*"

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Zip Plugin
        run: |
          zip -r christendom-sso.zip . -x "*.git*" -x "*.github*"
      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          files: christendom-sso.zip